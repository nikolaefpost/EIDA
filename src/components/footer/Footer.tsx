import React, {FC} from 'react';

import styles from "./footer.module.scss";

const Footer: FC = () => {
    return (
        <footer>
            Footer
        </footer>
    );
};

export default Footer;